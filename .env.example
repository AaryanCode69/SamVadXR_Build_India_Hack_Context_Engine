# ═══════════════════════════════════════════════════════
#  Samvad XR — Dev A Environment Variables
# ═══════════════════════════════════════════════════════
#  Copy this file to .env and fill in real values.
#  NEVER commit .env to git.
#
#  Architecture v3.0: Dev A owns AI + State only.
#  STT/TTS/RAG/Memory config belongs to Dev B.
# ═══════════════════════════════════════════════════════

# ── Required secrets ──────────────────────────────────
OPENAI_API_KEY=sk-your-openai-key-here

# ── AI model ──────────────────────────────────────────
OPENAI_MODEL=gpt-4o
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=200

# ── Feature flags ────────────────────────────────────
# USE_MOCKS controls Dev A's own mocks (OpenAI, Neo4j).
# Dev B's mocks (STT/TTS/RAG) are their concern.
USE_MOCKS=true

# ── Timeouts (milliseconds) — Dev A only ─────────────
AI_TIMEOUT_MS=10000

# ── Neo4j ────────────────────────────────────────────
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password-here
NEO4J_TIMEOUT_MS=2000

# ── Game rules ───────────────────────────────────────
MAX_TURNS=30
MAX_MOOD_DELTA=15

# ── Logging ──────────────────────────────────────────
LOG_LEVEL=DEBUG

# ── Dev server (not used in production) ──────────────
APP_VERSION=0.1.0
CORS_ORIGINS=*
